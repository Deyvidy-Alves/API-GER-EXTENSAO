model ItemOrcamento {
  id    String         @id @default(uuid())
  conta ContaOrcamento
  valor Decimal        @db.Decimal(10, 2)

  cursoId String
  curso   CursoExtensao @relation(fields: [cursoId], references: [id])

  @@unique([cursoId, conta])
  @@map("itens_orcamento")
}
